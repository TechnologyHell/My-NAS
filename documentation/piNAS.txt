### install raspbian os on micro sd card using Raspberry Pi Imager
Operating System : Raspberry Pi OS (Legacy) (just because it is the most lightweight variant with almost all features)


### configure these details before you flash the operating system onto the micro sd card
Username : pi (easy to use with further terminal codes)
Password :  (will be required to access the pi from any network device)


### create a folder in home directory, which will later be shared
Folder Name : NEO


###create an account that will be used for accessing the storage contents
Samba User : root (idk but the pi doesnt accept any other username)
Password : pass#1345 (anything simple to remember)









